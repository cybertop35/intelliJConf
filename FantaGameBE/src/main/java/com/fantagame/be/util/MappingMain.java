package com.fantagame.be.util;


public class MappingMain {

	/**
	 * @param args
	 */

	public static void main(String[] args) {
	/*
	 * 	MappingTool tool = null;
	 
		XMLContext context = new XMLContext();
		context.setProperty(XMLConstants.DESCRIPTOR_PACKAGE, "");

		tool = context.createMappingTool();
		try {
			ByteArrayOutputStream stream=new ByteArrayOutputStream();
			OutputStreamWriter writer=new OutputStreamWriter(stream);
			
			List<Class> classes = ReflectionUtils.findClasses(ReflectionUtils.ONLY_CLASS,
									"com.fantagame.be.ws.personWSBean","com.fantagame.be.business.data.bean" );
			
		//	for (Class _class:classes)			
		//		tool.addClass(_class,true,WSConstant.NAME_SPACE);			
						
			
			tool.write(writer);
			FileUtil.writeXML(stream.toString(), new File("mapping.xml"));
	        
		} catch (MappingException e) {
			e.printStackTrace();
		} catch (Exception e) {
			e.printStackTrace();
		}
		*/
	}	
	
}
