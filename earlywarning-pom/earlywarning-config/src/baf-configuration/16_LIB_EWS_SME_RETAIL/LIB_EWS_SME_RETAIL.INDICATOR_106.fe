VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_106.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "


CAST(
	CASE
		WHEN NUM_RATE_IMPAGATE IS NULL OR NUM_RATE_IMPAGATE < 0 THEN NULL
		ELSE NUM_RATE_IMPAGATE
	END AS DECIMAL(10,0)) AS IND_106,
	
	CASE
		WHEN NUM_RATE_IMPAGATE IS NULL THEN 'RTI000_1'
		ELSE ''
	END AS ERROR_MSG_IND_106
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;