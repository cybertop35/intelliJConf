VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_108.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
CAST(
	CASE
		WHEN IMP_ARRETRATO IS NULL OR IMP_ARRETRATO < 0 THEN NULL
		ELSE IMP_ARRETRATO
	END AS DECIMAL(18,6)) AS IND_108,
	
	CASE
		WHEN IMP_ARRETRATO IS NULL THEN 'RTI000_1'
		WHEN IMP_ARRETRATO < 0 THEN 'RTI000_2'
		ELSE ''
	END AS ERROR_MSG_IND_108
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;