VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_107.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

 

CAST(
	CASE
		WHEN NUM_GG_SCADUTO_RI IS NULL OR NUM_GG_SCADUTO_RI < 0 THEN NULL
		ELSE NUM_GG_SCADUTO_RI
	END AS DECIMAL(10,0)) AS IND_107,
	
	CASE
		WHEN NUM_GG_SCADUTO_RI IS NULL THEN 'RTI000_1'
		WHEN NUM_GG_SCADUTO_RI < 0 THEN 'RTI000_2'
		ELSE ''
	END AS ERROR_MSG_IND_107


";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;