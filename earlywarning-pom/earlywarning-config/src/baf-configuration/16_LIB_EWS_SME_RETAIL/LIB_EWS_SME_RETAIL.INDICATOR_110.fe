VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_110.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
CAST(
	CASE
		WHEN NUM_GG_SCADUTO_RIDMAV IS NULL THEN NULL
		ELSE NUM_GG_SCADUTO_RIDMAV
	END AS DECIMAL(10,0)) AS IND_110,
	
	CASE
		WHEN NUM_GG_SCADUTO_RIDMAV IS NULL THEN 'RTI000_5'
		ELSE ''
	END AS ERROR_MSG_IND_110
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;