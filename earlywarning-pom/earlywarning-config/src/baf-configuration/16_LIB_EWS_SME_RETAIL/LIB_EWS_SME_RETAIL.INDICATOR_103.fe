VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_103.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(
	CASE 
		WHEN FLG_IRIS_GAR_NDG_TOT IS NULL THEN '0'
			
		WHEN FLG_IRIS_GAR_NDG_TOT = '1' THEN '1'
			ELSE '0' 
	END AS CHAR(1)) as IND_103,

CASE
	WHEN FLG_IRIS_GAR_NDG_TOT IS NULL THEN 'SGR000_1'
	ELSE '' 
END as ERROR_MSG_IND_103



";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql; 
