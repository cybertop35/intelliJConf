VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_102.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
CAST(
CASE
	WHEN FLG_IRIS_GAR_TOT IS NULL THEN '0'
	ELSE FLG_IRIS_GAR_TOT 
END as char(1)) as IND_102,

CASE
	WHEN FLG_IRIS_GAR_TOT IS NULL THEN 'SGR000_1' 
	ELSE '' 
END as ERROR_MSG_IND_102


";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql; 
