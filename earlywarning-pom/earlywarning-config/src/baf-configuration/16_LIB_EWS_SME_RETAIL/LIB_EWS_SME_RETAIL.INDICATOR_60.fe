VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_60.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(
	CASE 
		WHEN IND_65 IS NULL AND
			 IND_66 IS NULL AND
			 IND_218 IS NULL AND
			 IND_219 IS NULL THEN '0'
		ELSE '1'
	END AS CHAR(1)) AS IND_60,
	

	CAST(NULL AS VARCHAR(20)) AS ERROR_MSG_IND_60
		

";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;

