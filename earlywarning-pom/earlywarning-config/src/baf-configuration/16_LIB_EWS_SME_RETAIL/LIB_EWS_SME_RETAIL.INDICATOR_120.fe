VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_120.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
CAST(
	CASE
		WHEN IMP_DEBITO_RESIDUO_RI IS NULL OR IMP_DEBITO_RESIDUO_RI < 0 THEN NULL
		ELSE IMP_DEBITO_RESIDUO_RI
	END AS DECIMAL(18,6)) AS IND_120,
	
	CASE
		WHEN IMP_DEBITO_RESIDUO_RI IS NULL THEN 'RTI000_1'
		WHEN IMP_DEBITO_RESIDUO_RI < 0 THEN 'RTI000_2'
		ELSE ''
	END AS ERROR_MSG_IND_120
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;