VAR formulaName = "[LIB_EWS_IT.INDICATOR_217.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "


CAST(
	CASE 
		WHEN OVERDRAFT_DD_3M_SS IS NULL THEN NULL 
		WHEN OVERDRAFT_DD_3M_SS < 0 THEN 9999999
		ELSE OVERDRAFT_DD_3M_SS
	END AS DECIMAL(10,0)) AS IND_217,

 
	CASE 
		WHEN(OVERDRAFT_DD_3M_SS IS NULL OR OVERDRAFT_DD_3M_SS < 0 ) THEN 'XRA001' 
		ELSE ''
	END AS ERROR_MSG_IND_217 

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
