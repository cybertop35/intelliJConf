VAR formulaName = "[LIB_EWS_IT.INDICATOR_72.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(
	CASE 
		WHEN(PREAM_EXP_FIN_MM IS NULL OR PREAM_EXP_FIN_MM < 0 )  THEN 9999999
		ELSE PREAM_EXP_FIN_MM
	END AS DECIMAL(10,0)) AS IND_72,

	CASE 
		WHEN(PREAM_EXP_FIN_MM IS NULL OR PREAM_EXP_FIN_MM < 0 ) THEN 'PREAM001' 
		ELSE ''
	END AS ERROR_MSG_IND_72
";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
