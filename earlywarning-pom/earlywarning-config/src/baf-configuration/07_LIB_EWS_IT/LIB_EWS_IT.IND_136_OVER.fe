VAR formulaName = "[LIB_EWS_IT.IND_136_OVER.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
CAST(
		CASE WHEN VAL_BR_136_OVERRIDE is not null THEN (IND_39 - VAL_BR_136_OVERRIDE)
			 ELSE null
		END AS DECIMAL(30,12)) AS IND_136_OVER,
		
		(CASE
			WHEN VAL_BR_136_OVERRIDE is not null THEN ''
			 ELSE ''
		END ) AS ERROR_MSG_IND_136_OVER
";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
