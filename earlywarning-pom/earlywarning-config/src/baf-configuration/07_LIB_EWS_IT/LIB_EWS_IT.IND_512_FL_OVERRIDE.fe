VAR formulaName = "[LIB_EWS_IT.IND_512_FL_OVERRIDE.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
CAST(
		CASE 
			WHEN BR_512_OVERRIDE  = '0' AND (IND_12) < (VAL_BR_512_OVERRIDE*1.02) AND (IND_12 < 0.1) THEN '1'
              ELSE '0'  
		END AS CHAR(1)) AS IND_512_FL_OVERRIDE,
		
		(CASE
			WHEN BR_512_OVERRIDE  = '0' AND (IND_12) < (VAL_BR_512_OVERRIDE*1.02) AND (IND_12 < 0.1) THEN '1'
			ELSE ''
		END ) AS ERROR_MSG_IND_512_FL_OVERRIDE
";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;