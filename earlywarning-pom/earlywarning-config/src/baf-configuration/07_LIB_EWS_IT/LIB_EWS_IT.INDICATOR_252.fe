VAR formulaName = "[LIB_EWS_IT.INDICATOR_252.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(
	CASE	
		WHEN AMT_FAT_OVERDUE_DEB_M0 IS NULL THEN NULL
		WHEN AMT_FAT_OVERDUE_DEB_M0 <0 THEN 9999999
		ELSE AMT_FAT_OVERDUE_DEB_M0
	END AS DECIMAL(30,12)) AS IND_252,

	CASE
		WHEN (AMT_FAT_OVERDUE_DEB_M0 IS NULL OR AMT_FAT_OVERDUE_DEB_M0 <0) THEN 'NPAF001'
		ELSE ''
	END AS ERROR_MSG_IND_252 

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
