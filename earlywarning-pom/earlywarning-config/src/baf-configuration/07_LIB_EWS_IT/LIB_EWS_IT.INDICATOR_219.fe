VAR formulaName = "[LIB_EWS_IT.INDICATOR_219.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "


CAST(
	CASE 
		WHEN(OVERDRAFT_DD_3M IS NULL OR OVERDRAFT_DD_3M < 0 )  THEN 0
		ELSE 1
	END AS CHAR(1)) AS IND_219,

	CASE 
		WHEN(OVERDRAFT_DD_3M IS NULL OR OVERDRAFT_DD_3M < 0 ) THEN 'XRA001' 
		ELSE ''
	END AS ERROR_MSG_IND_219 

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
