VAR formulaName = "[LIB_EWS_IT.INDICATOR_218.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "


CAST(
	CASE 
		WHEN(OVERDRAFT_DD_3M_SS IS NULL OR OVERDRAFT_DD_3M_SS < 0 )  THEN 0
		ELSE 1
	END AS CHAR(1)) AS IND_218,

	CASE 
		WHEN(OVERDRAFT_DD_3M_SS IS NULL OR OVERDRAFT_DD_3M_SS < 0 ) THEN 'XRA001' 
		ELSE ''
	END AS ERROR_MSG_IND_218 

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
