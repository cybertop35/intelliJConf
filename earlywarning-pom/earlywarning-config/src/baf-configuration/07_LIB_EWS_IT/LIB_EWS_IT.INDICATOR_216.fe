VAR formulaName = "[LIB_EWS_IT.INDICATOR_216.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "


CAST(
	CASE 
		WHEN AMT_OVERDRAFT_1M IS NULL THEN NULL
		WHEN AMT_OVERDRAFT_1M < 0 THEN -9999999
		ELSE AMT_OVERDRAFT_1M
	END AS DECIMAL(30,12)) AS IND_216,

	CASE 
		WHEN AMT_OVERDRAFT_1M IS NULL THEN 'XRA000_1'
		WHEN AMT_OVERDRAFT_1M < 0 THEN 'XRA004_1'
		ELSE ''
	END AS ERROR_MSG_IND_216 
";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
