VAR formulaName = "[LIB_EWS_IT.INDICATOR_31.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
	CAST(
		CASE
			WHEN JD_DEBT_PUBBLI IS NULL THEN 0
            ELSE JD_DEBT_PUBBLI
		END AS CHAR(1)
		) AS IND_31
		";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;