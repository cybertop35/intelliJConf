VAR formulaName = "[LIB_EWS_IT.INDICATOR_233.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
CAST (CASE
			WHEN AMT_OVERDRAFT_SS90_U_T IS NULL THEN NULL
			WHEN AMT_OVERDRAFT_SS90_U_T < 0 THEN AMT_OVERDRAFT_SS90_U_T *(-1)
			ELSE AMT_OVERDRAFT_SS90_U_T
	END AS DECIMAL(30,6)) AS IND_233,
	
	CASE
			WHEN AMT_OVERDRAFT_SS90_U_T IS NULL THEN 'XRA001' ELSE ''
	END AS ERROR_MSG_IND_233
		";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
