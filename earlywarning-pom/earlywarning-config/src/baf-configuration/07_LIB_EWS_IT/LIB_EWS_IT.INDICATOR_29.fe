VAR formulaName = "[LIB_EWS_IT.INDICATOR_29.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
	CAST(
		CASE
			WHEN JD_ACC_RISTRUT IS NULL THEN 0
            ELSE JD_ACC_RISTRUT
		END AS CHAR(1)
		) AS IND_29,
		NULL AS ERROR_MSG_IND_29
		";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;