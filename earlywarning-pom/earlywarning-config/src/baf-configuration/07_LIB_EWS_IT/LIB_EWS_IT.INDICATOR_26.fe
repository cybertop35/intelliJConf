VAR formulaName = "[LIB_EWS_IT.INDICATOR_26.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
	CAST(
		CASE
			WHEN JD_DECANAL_PTF IS NULL THEN 0
            ELSE JD_DECANAL_PTF
		END AS CHAR(1)
		) AS IND_26,
		NULL AS ERROR_MSG_IND_26
		";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;