VAR formulaName = "[LIB_EWS_IT.INDICATOR_214.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(
	CASE 
		WHEN OVERDRAFT_DD_OVER_SS IS NULL THEN -1000000
		ELSE OVERDRAFT_DD_OVER_SS
	END AS DECIMAL(10,0)) AS IND_214,
  
 CASE 
	WHEN OVERDRAFT_DD_OVER_SS IS NULL THEN 'XRA001' 
	ELSE ''
END AS ERROR_MSG_IND_214
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;