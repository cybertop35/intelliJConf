VAR formulaName = "[LIB_EWS_IT.INDICATOR_213.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(
	CASE 
		WHEN OVERDRAFT_DD_OVER_NOSO IS NULL THEN NULL
		WHEN OVERDRAFT_DD_OVER_NOSO < 0 THEN 9999999
		ELSE OVERDRAFT_DD_OVER_NOSO
	END AS DECIMAL(10,0)) AS IND_213,
  
 CASE 
	WHEN OVERDRAFT_DD_OVER_NOSO IS NULL THEN 'XRA001' 
	ELSE ''
END AS ERROR_MSG_IND_213
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;