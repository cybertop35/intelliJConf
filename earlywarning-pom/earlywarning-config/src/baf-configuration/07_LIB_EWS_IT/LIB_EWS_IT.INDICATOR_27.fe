VAR formulaName = "[LIB_EWS_IT.INDICATOR_27.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
	CAST(
		CASE
			WHEN JD_REATI_GRAVI IS NULL THEN 0
            ELSE JD_REATI_GRAVI
		END AS CHAR(1)
		) AS IND_27
		";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;