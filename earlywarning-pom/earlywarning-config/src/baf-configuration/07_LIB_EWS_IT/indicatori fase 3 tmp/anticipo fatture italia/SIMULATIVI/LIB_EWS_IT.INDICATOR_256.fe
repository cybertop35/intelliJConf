VAR formulaName = "[LIB_EWS_IT.INDICATOR_256.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(
	CASE	
		WHEN AMT_FAT_OVERDUE_DEB_M0 IS NULL OR
			 AMT_FAT_OVERDUE_DEB_M1 IS NULL OR
			 AMT_FAT_OVERDUE_DEB_M2 IS NULL THEN -1000000
			 ELSE (AMT_FAT_OVERDUE_DEB_M0 + AMT_FAT_OVERDUE_DEB_M1 + AMT_FAT_OVERDUE_DEB_M2)/3
	END AS DECIMAL(18,6)) AS IND_256,

	CASE
		WHEN AMT_FAT_OVERDUE_DEB_M0 IS NULL OR
			 AMT_FAT_OVERDUE_DEB_M1 IS NULL OR
			 AMT_FAT_OVERDUE_DEB_M2 IS NULL THEN 'NPAF001'
			 ELSE ''
	END AS ERROR_MSG_IND_256 

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
