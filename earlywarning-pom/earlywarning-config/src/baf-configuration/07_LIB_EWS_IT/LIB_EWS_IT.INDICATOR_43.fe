VAR formulaName = "[LIB_EWS_IT.INDICATOR_43.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "


CAST(
	CASE 
		WHEN MASTER_SCALE_RATING_FITCH IS NULL THEN NULL
		ELSE MASTER_SCALE_RATING_FITCH
	END AS DECIMAL(10,0)) AS IND_43,

	CASE 
		WHEN MASTER_SCALE_RATING_FITCH IS NULL THEN 'E0001'
		ELSE ''
	END AS ERROR_MSG_IND_43 

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
