VAR formulaName = "[LIB_EWS_IT.INDICATOR_238.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(
	CASE	
		WHEN (AMT_FAT_OVERDUE_M0 IS NULL OR AMT_FAT_OVERDUE_M0 <0) THEN -1000000
		ELSE AMT_FAT_OVERDUE_M0
	END AS DECIMAL(30,6)) AS IND_238,

	CASE
		WHEN (AMT_FAT_OVERDUE_M0 IS NULL OR AMT_FAT_OVERDUE_M0 <0) THEN 'NPAF001'
			 ELSE ''
	END AS ERROR_MSG_IND_238 

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
