VAR formulaName = "[LIB_EWS_IT.INDICATOR_28.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
	CAST(
		CASE
			WHEN JD_CONCOR_RICH IS NULL THEN 0
            ELSE JD_CONCOR_RICH
		END AS CHAR(1)
		) AS IND_28,
		NULL AS ERROR_MSG_IND_28
		";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;