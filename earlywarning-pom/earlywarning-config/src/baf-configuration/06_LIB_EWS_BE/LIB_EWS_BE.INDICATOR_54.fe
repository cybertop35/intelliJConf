VAR formulaName = "[LIB_EWS_BE.INDICATOR_54.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_NON_PERFORMING_EXPOSURE,'') IS NULL THEN '0'
		ELSE FLG_NON_PERFORMING_EXPOSURE 
	END AS CHAR(1)) AS IND_54,

CASE
	WHEN NULLIF(FLG_NON_PERFORMING_EXPOSURE,'') IS NULL THEN 'E0001' ELSE ''
END ERROR_MSG_IND_54


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
