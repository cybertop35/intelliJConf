VAR formulaName = "[LIB_EWS_BE.INDICATOR_223.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	 (CASE 
			WHEN NULLIF(FLG_DELTA_CASHFLOW,'') IS NULL THEN '0'
			ELSE FLG_DELTA_CASHFLOW
	  END AS CHAR(1)) AS IND_223,

CASE
 	WHEN NULLIF(FLG_DELTA_CASHFLOW,'') IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_223




";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
