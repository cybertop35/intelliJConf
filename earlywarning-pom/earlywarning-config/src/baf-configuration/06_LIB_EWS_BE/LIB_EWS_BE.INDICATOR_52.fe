VAR formulaName = "[LIB_EWS_BE.INDICATOR_52.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_BILLS_CHEQUES_REJECTION,'') IS NULL THEN 0
		ELSE FLG_BILLS_CHEQUES_REJECTION 
	END AS CHAR(1)) AS IND_52,

CASE
	WHEN NULLIF(FLG_BILLS_CHEQUES_REJECTION,'') IS NULL THEN 'E0001' ELSE ''
END ERROR_MSG_IND_52


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
