VAR formulaName = "[LIB_EWS_BE.INDICATOR_18.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_RESTRUCT_REQUEST,'') IS NULL THEN 0
		ELSE FLG_RESTRUCT_REQUEST 
	END AS CHAR(1)) AS IND_18,

CASE
	WHEN NULLIF(FLG_RESTRUCT_REQUEST,'') IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_18


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
