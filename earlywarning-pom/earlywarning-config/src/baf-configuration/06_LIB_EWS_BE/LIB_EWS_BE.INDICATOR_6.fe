VAR formulaName = "[LIB_EWS_BE.INDICATOR_6.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "


CAST
	 (CASE 
		WHEN NULLIF(FLG_CREDIT_REVOKED,'') IS NULL THEN '0'
		ELSE FLG_CREDIT_REVOKED 
	END AS CHAR(1)) as IND_6,

CASE
 	WHEN NULLIF(FLG_CREDIT_REVOKED,'') IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_6

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
