VAR formulaName = "[LIB_EWS_BE.INDICATOR_207.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "



CASE 
	WHEN UNPAID_INSTAL_LEASING_CONTR IS NULL OR UNPAID_INSTAL_LEASING_CONTR < 0 THEN 0
	ELSE cast(UNPAID_INSTAL_LEASING_CONTR AS DECIMAL(10,0))
END AS IND_207,

CASE
	WHEN UNPAID_INSTAL_LEASING_CONTR IS NULL OR UNPAID_INSTAL_LEASING_CONTR < 0 THEN 'E0001' ELSE ''
END ERROR_MSG_IND_207


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
