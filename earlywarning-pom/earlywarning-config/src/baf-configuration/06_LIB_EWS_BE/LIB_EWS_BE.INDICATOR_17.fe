VAR formulaName = "[LIB_EWS_BE.INDICATOR_17.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	 (CASE 
		WHEN NULLIF(FLG_CRR_DEFAULT,'') IS NULL THEN 0
		ELSE FLG_CRR_DEFAULT 
		END AS CHAR(1)) AS IND_17,

CASE
 	WHEN NULLIF(FLG_CRR_DEFAULT,'') IS NULL THEN 'E0001 ;' ELSE ''
END AS ERROR_MSG_IND_17


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
