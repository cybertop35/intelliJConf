VAR formulaName = "[LIB_EWS_BE.INDICATOR_30.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_COURT_REGISTER_DELETE,'') IS NULL THEN 0
		ELSE FLG_COURT_REGISTER_DELETE 
	END AS CHAR(1)) AS IND_30,

CASE
 	WHEN NULLIF(FLG_COURT_REGISTER_DELETE,'') IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_30

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
