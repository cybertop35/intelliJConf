VAR formulaName = "[LIB_EWS_BE.INDICATOR_29.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_TAX_CHANGE,'') IS NULL THEN '0'
		ELSE FLG_TAX_CHANGE 
	END AS CHAR(1)) AS IND_29,

CASE
	WHEN NULLIF(FLG_TAX_CHANGE,'') IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_29


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
