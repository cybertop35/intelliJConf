VAR formulaName = "[LIB_EWS_BE.INDICATOR_55.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "CASE WHEN FORBORNE_NPE  IS NULL THEN 0
ELSE (CAST(CAST(FORBORNE_NPE AS INTEGER) AS CHAR(1))) END AS IND_55,
CASE WHEN FORBORNE_NPE IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_55";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
