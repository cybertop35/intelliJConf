VAR formulaName = "[LIB_EWS_BE.INDICATOR_208.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "


CASE 
	WHEN UNPAID_INSTAL_AMORTIZ_PROD IS NULL THEN MISSING_VAL_IND_208
	ELSE CAST(UNPAID_INSTAL_AMORTIZ_PROD AS DECIMAL(10,0))
END AS IND_208,

CASE
	WHEN UNPAID_INSTAL_AMORTIZ_PROD IS NULL THEN 'E0001 ' ELSE ''
END ERROR_MSG_IND_208


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
