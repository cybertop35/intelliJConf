VAR formulaName = "[LIB_EWS_BE.INDICATOR_209.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

-999 AS MISSING_VALUE_IND_209,

CASE 
	WHEN UNPAID_INSTAL_LOANS IS NULL THEN 0
	ELSE CAST(UNPAID_INSTAL_LOANS AS DECIMAL(18,6))
END AS IND_209,

CASE
	WHEN UNPAID_INSTAL_LOANS IS NULL THEN 'E0001 ' ELSE ''
END ERROR_MSG_IND_209


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
