VAR formulaName = "[LIB_EWS_BE.INDICATOR_24.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	 (CASE 
		WHEN NULLIF(FLG_OPEN_EXECUTION,'') IS NULL THEN 0
		ELSE 1 
	END AS CHAR(1)) AS IND_24,

CASE
 	WHEN NULLIF(FLG_OPEN_EXECUTION,'') IS NULL THEN 'E0001 ;' ELSE ''
END AS ERROR_MSG_IND_24

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
