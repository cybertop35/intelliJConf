VAR formulaName = "[LIB_EWS_BE.INDICATOR_176.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST (CASE
				WHEN CHEQUES_UNPAID_M0 IS NULL THEN 0
				WHEN CHEQUES_UNPAID_M0 > 0 THEN 1
			ELSE 0
	  END AS CHAR(1)) AS IND_176,
	  
	(CASE
				WHEN CHEQUES_UNPAID_M0 IS NULL THEN 'E0001 ;' ELSE ''
	 END ) AS ERROR_MSG_IND_176

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;

