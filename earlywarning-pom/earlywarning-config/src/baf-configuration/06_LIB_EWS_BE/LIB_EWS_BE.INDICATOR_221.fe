VAR formulaName = "[LIB_EWS_BE.INDICATOR_221.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	 (CASE 
			WHEN NULLIF(FLG_PAST_DUE_CREDIT_EMPL ,'') IS NULL THEN 0
			ELSE FLG_PAST_DUE_CREDIT_EMPL
	  END AS CHAR(1)) AS IND_221,

CASE
 	WHEN NULLIF(FLG_PAST_DUE_CREDIT_EMPL,'') IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_221




";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
