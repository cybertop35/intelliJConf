VAR formulaName = "[LIB_EWS_BE.INDICATOR_36.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_CLIENT_EXT_ACCOUNT,'') IS NULL THEN 0
		ELSE FLG_CLIENT_EXT_ACCOUNT 
	END AS CHAR(1)) AS IND_36,

CASE
	WHEN NULLIF(FLG_CLIENT_EXT_ACCOUNT,'') IS NULL THEN 'E0001' ELSE ''
END ERROR_MSG_IND_36


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
