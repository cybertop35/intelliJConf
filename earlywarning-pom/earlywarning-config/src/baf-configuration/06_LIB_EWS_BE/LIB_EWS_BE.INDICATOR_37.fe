VAR formulaName = "[LIB_EWS_BE.INDICATOR_37.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_GUARANTOR_EXT_ACCOUNT,'') IS NULL THEN '0'
		ELSE FLG_GUARANTOR_EXT_ACCOUNT 
	END AS CHAR(1)) AS IND_37,

CASE
 	WHEN NULLIF(FLG_GUARANTOR_EXT_ACCOUNT,'') IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_37

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
