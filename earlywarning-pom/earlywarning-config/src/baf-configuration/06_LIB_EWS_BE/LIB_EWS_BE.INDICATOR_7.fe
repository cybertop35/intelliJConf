VAR formulaName = "[LIB_EWS_BE.INDICATOR_7.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "(CASE 
		WHEN EXPOSURE_AMOUNT = 0 THEN NULL
		ELSE (OVERDUE_AMOUNT / (EXPOSURE_AMOUNT)) 
	   END ) AS IND_7";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
