VAR formulaName = "[LIB_EWS_BE.INDICATOR_25.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_EXTERNAL_NEGATIVE_INFO,'') IS NULL THEN 0
		ELSE FLG_EXTERNAL_NEGATIVE_INFO 
	END AS CHAR(1))AS IND_25,

CASE
	WHEN NULLIF(FLG_EXTERNAL_NEGATIVE_INFO,'') IS NULL THEN 'E0001 ;' ELSE ''
END AS ERROR_MSG_IND_25


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
