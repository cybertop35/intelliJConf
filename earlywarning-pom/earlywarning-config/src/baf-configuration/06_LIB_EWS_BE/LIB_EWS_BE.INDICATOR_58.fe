VAR formulaName = "[LIB_EWS_BE.INDICATOR_58.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(CASE
	WHEN MONTHS_WITH_OVERDUE IS NULL OR MONTHS_WITH_OVERDUE < 0 THEN MISSING_VAL_IND_58
	ELSE (MONTHS_WITH_OVERDUE) 
END AS DECIMAL(10,0)) AS IND_58,

(CASE
	WHEN MONTHS_WITH_OVERDUE IS NULL OR MONTHS_WITH_OVERDUE < 0 THEN 'E0001' ELSE ''
END
) AS ERROR_MSG_IND_58

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
