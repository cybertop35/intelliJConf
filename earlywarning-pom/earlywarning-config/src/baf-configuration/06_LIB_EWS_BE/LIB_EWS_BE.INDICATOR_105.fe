VAR formulaName = "[LIB_EWS_BE.INDICATOR_105.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "(CASE 
	WHEN SALES_Y0 = 0 THEN NULL 
	ELSE (MATERIAL_COSTS_Y0/SALES_Y0) 
END) AS IND_105";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
