VAR formulaName = "[LIB_EWS_BE.INDICATOR_225.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	 (CASE 
			WHEN NULLIF(FLG_BOND_SUSPENDED,'') IS NULL THEN 0
			ELSE FLG_BOND_SUSPENDED
	  END AS CHAR(1)) AS IND_225,

CASE
 	WHEN NULLIF(FLG_BOND_SUSPENDED,'') IS NULL THEN 'E0001 ' ELSE ''
END AS ERROR_MSG_IND_225

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
