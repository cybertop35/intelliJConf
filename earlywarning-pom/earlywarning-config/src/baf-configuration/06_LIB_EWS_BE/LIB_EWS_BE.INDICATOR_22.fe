VAR formulaName = "[LIB_EWS_BE.INDICATOR_22.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "


CAST
	 (CASE 
		WHEN NULLIF(FLG_UNFULFILLED_PAYMENTS,'') IS NULL THEN 0
		ELSE 1 
	END AS CHAR(1)) AS IND_22,

CASE
 	WHEN NULLIF(FLG_UNFULFILLED_PAYMENTS,'') IS NULL THEN 'E0001 ;' ELSE ''
END AS ERROR_MSG_IND_22


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;