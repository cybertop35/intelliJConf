VAR formulaName = "[LIB_EWS_BE.INDICATOR_224.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "


CAST
	 (CASE 
			WHEN NULLIF(COVENANT_BREACH,'') IS NULL THEN '0'
			ELSE COVENANT_BREACH
	  END AS CHAR(1)) AS IND_224,

CASE
 	WHEN NULLIF(COVENANT_BREACH,'') IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_224




";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
