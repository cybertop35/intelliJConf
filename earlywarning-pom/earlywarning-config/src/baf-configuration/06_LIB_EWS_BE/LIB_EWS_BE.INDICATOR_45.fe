VAR formulaName = "[LIB_EWS_BE.INDICATOR_45.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	 (CASE 
		WHEN NULLIF(FLG_CREDIT_COVENANT,'') IS NULL THEN 0
		ELSE FLG_CREDIT_COVENANT 
	END AS CHAR(1)) AS IND_45,

CASE
 	WHEN NULLIF(FLG_CREDIT_COVENANT,'') IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_45

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;