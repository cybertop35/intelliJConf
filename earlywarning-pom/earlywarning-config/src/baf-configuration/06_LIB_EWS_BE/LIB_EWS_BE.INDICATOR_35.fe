VAR formulaName = "[LIB_EWS_BE.INDICATOR_35.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "(CASE 
	WHEN EQUITY_Y1 = 0 THEN NULL
	ELSE ((EQUITY_Y0/EQUITY_Y1)-1)
END) AS ID35";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
