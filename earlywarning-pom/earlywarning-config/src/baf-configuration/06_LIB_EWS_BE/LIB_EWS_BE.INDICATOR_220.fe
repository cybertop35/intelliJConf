VAR formulaName = "[LIB_EWS_BE.INDICATOR_220.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	 (CASE 
			WHEN NULLIF(FLG_MATERIALITY_THRESHOLD,'') IS NULL THEN 0
			ELSE FLG_MATERIALITY_THRESHOLD
	  END AS CHAR(1)) AS IND_220,

CASE
 	WHEN NULLIF(FLG_MATERIALITY_THRESHOLD,'') IS NULL THEN 'E0001 ;' ELSE ''
END AS ERROR_MSG_IND_220


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
