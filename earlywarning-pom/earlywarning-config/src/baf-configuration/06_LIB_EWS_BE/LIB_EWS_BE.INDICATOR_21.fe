VAR formulaName = "[LIB_EWS_BE.INDICATOR_21.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_UNFULFILLED_COVENANT,'') IS NULL THEN '0'
		ELSE FLG_UNFULFILLED_COVENANT 
	END AS CHAR(1)) AS IND_21,

CASE
	WHEN NULLIF(FLG_UNFULFILLED_COVENANT,'') IS NULL THEN 'E0001' ELSE ''
END AS ERROR_MSG_IND_21


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
