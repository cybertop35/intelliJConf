VAR formulaName = "[LIB_EWS_BE.INDICATOR_49.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "CAST(CAST(CASE
                WHEN FLG_BANKRUPT_APPL_DEBTOR IS NULL THEN 0
                ELSE FLG_BANKRUPT_APPL_DEBTOR 
END AS INTEGER) AS CHAR(1)) AS IND_49,
CASE
WHEN FLG_BANKRUPT_APPL_DEBTOR IS NULL THEN 'ERROR; ' ELSE ''
END AS ERROR_MSG_IND_49";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
