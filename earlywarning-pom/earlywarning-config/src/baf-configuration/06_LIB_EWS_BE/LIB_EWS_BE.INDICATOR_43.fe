VAR formulaName = "[LIB_EWS_BE.INDICATOR_43.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_INSOLVENT_GUARANTOR,'') IS NULL THEN 0
		ELSE FLG_INSOLVENT_GUARANTOR 
	END AS CHAR(1)) AS IND_43,

CASE
	WHEN NULLIF(FLG_INSOLVENT_GUARANTOR,'') IS NULL THEN 'E0001 ;' ELSE ''
END ERROR_MSG_IND_43


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
