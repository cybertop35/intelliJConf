VAR formulaName = "[LIB_EWS_BE.INDICATOR_173.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "CAST(CASE 
	WHEN NULLIF(OUTFLOWS_AMOUNT_M0,'') IS NULL THEN 0
	WHEN NULLIF(OUTFLOWS_AMOUNT_M1,'') IS NULL THEN 0
	WHEN OUTFLOWS_AMOUNT_M0 < OUTFLOWS_AMOUNT_M1 THEN 1
	ELSE 0
END AS CHAR(1)) AS IND_173";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
