VAR formulaName = "[LIB_EWS_BE.INDICATOR_23.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST
	(CASE 
		WHEN NULLIF(FLG_DELINQUENCY,'') IS NULL THEN 0
		ELSE FLG_DELINQUENCY 
	END AS CHAR(1))AS IND_23,

CASE
	WHEN NULLIF(FLG_DELINQUENCY,'') IS NULL THEN 'E0001 ;' ELSE ''
END AS ERROR_MSG_IND_23


";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;
