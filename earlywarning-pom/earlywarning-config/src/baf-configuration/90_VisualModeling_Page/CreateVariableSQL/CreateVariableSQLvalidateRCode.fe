var formulaName = "[CreateVariableSQL.validateRCode] ";
DEBUGMESSAGE(formulaName, "START");

var rCode = Param1;
var newVariableName = Param2;

var codeIsOk = true;
if (not contains(rCode, "df$" + newVariableName)) {
    codeIsOk = false;
}

DEBUGMESSAGE(formulaName, "END");
return codeIsOk;
