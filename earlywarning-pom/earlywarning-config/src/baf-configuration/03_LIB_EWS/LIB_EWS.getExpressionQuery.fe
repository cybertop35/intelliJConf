VAR FT = Param1;
VAR ID = Param2;
var expression;

IF (FT == NULL OR ID == NULL) THEN RETURN '';

expression = FT.FastTrack_Score[Categorical_Score.ID=$ID].ExpressionQuery;

IF(SIZE(expression) > 0 ) THEN {
	return TEXTFILETOSTRING(expression[0]);
}

return "";