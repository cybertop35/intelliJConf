# FENull returnNull LIB_RISK.updateAnalyticaltable;
# RETURN VALUE: FENull      -
# EFFETCS:      -           updateAnalyticaltable
# Param1:       FEInstance      analysisUnit instance id

DEBUGMESSAGE("LIB_RISK_EW.updateAnalysisUnit.fe"); # stacktrace

VAR analysisUnitID 	= Param1;
VAR logActionName	= "BackTesting";
VAR result 			= null;

SAVE_LOG("Info", updateAnalysisUnit, "Start Update AnalysisUnit Generica");

IF (analysisUnitID == "CUSTOMER") THEN {
	
	result = LIB_EWS_IT.updateAnalysisUnit_IT(analysisUnitID);
	
	if(result <> NULL) THE {
		SAVE_LOG("Info", updateAnalysisUnit, "Update AnalysisUnit Italia Completata");
		result = null;
	}
	else{
		SAVE_LOG("Error", updateAnalysisUnit, "Errore Update AnalysisUnit Italia");
	}		
}
ELSE IF (analysisUnitID == "CUSTOMER_BE") THEN {
	result = LIB_EWS_BE.updateOutputIrisTable_BE(analysisUnitID);
	
	if(result <> NULL) THE {
		SAVE_LOG("Info", updateAnalysisUnit, "Update AnalysisUnit Italia Banche estere");
		result = null;
	}
	else{
		SAVE_LOG("Error", updateAnalysisUnit, "Errore Update AnalysisUnit Banche estere");
	}
}
else {
	DEBUGMESSAGE("LIB_RISK_EW.updateAnalysisUnit.fe Formula not found "+analysisUnitID)
}

SAVE_LOG("Info", updateAnalysisUnit, "End Update AnalysisUnit Generica");

return now();
