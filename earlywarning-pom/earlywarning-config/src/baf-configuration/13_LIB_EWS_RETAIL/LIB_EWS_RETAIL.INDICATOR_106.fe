VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_106.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

NUM_COEF_IMPG_DIR 

CAST(
	CASE
		WHEN NUM_COEF_IMPG_DIR IS NULL THEN NULL
		ELSE NUM_COEF_IMPG_DIR
	END AS DECIMAL(18,6)) AS IND_106,
	
	CASE
		WHEN NUM_COEF_IMPG_DIR IS NULL THEN 'ESP000_1'
		ELSE ''
	END AS ERROR_MSG_IND_106
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;