VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_117.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(
	CASE
		WHEN IMP_SCONFINO_FORB IS NULL OR IMP_SCONFINO_FORB < 0 THEN NULL
		ELSE IMP_SCONFINO_FORB
	END AS DECIMAL(18,6)) AS IND_117,
	
	CASE
		WHEN IMP_SCONFINO_FORB IS NULL THEN 'FORB000_1'
		WHEN IMP_SCONFINO_FORB < 0 THEN 'FORB000_2'
		ELSE ''
	END AS ERROR_MSG_IND_117
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;