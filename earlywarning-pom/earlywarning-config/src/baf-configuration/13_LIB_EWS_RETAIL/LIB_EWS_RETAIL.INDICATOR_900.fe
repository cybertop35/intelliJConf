VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_900.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

cast(null as varchar(50)) as IND_900,
NULL as ERROR_MSG_IND_900

/*
CAST(
	CASE
		WHEN PRODUCT_MIX IS NULL THEN NULL
		ELSE PRODUCT_MIX
	END AS VARCHAR(50)) AS IND_900,

CASE 
	WHEN PRODUCT_MIX IS NULL THEN 'E0001'
	ELSE ''
END AS ERROR_MSG_IND_900
*/
";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;