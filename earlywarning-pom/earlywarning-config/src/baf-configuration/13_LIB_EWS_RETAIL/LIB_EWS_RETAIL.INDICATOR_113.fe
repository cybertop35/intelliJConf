VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_113.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
CAST(
	CASE
		WHEN ESP_FL_SCONFINO_CC IS NULL THEN NULL
		ELSE ESP_FL_SCONFINO_CC
	END AS DECIMAL(10,0)) AS IND_113,
	
	CASE
		WHEN ESP_FL_SCONFINO_CC IS NULL THEN 'ESP000_1'
		ELSE ''
	END AS ERROR_MSG_IND_113
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;