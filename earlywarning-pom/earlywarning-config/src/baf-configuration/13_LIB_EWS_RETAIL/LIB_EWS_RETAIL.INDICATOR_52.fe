VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_52.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
CAST(
	case 
		when IMP_SCONF_MEAN_90GG IS NULL OR IMP_SCONF_MEAN_90GG < 0 THEN NULL
		else IMP_SCONF_MEAN_90GG
	end AS DECIMAL(10,4)) AS IND_52,
CASE
	when IMP_SCONF_MEAN_90GG IS NULL THEN 'XRA000_1' 
	WHEN IMP_SCONF_MEAN_90GG < 0 THEN 'XRA004_1'
	else ''
END AS ERROR_MSG_IND_52
		";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;

