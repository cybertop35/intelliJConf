VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_207.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
CAST(
	CASE
		WHEN NUM_GIO_SCONFINO IS NULL THEN NULL
		ELSE NUM_GIO_SCONFINO
	END AS DECIMAL(10,4)) AS IND_207,

CASE
	WHEN NUM_GIO_SCONFINO IS NULL THEN 'XRA000_1'
	ELSE '' 
END AS ERROR_MSG_IND_207

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;