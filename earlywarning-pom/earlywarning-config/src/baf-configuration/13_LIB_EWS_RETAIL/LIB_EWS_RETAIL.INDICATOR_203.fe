VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_203.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
CAST(
	CASE
		WHEN NUM_GIO_SCONF_MAX_90GG IS NULL THEN NULL
		ELSE NUM_GIO_SCONF_MAX_90GG
	END AS DECIMAL(10,4)) AS IND_203,

CASE
	WHEN NUM_GIO_SCONF_MAX_90GG IS NULL THEN 'XRA000_1'
	ELSE '' 
END AS ERROR_MSG_IND_203

";


DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;