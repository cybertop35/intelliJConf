VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_107.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
CAST(
	CASE
		WHEN NUM_GG_SCADUTO_DIR IS NULL THEN NULL
		ELSE NUM_GG_SCADUTO_DIR
	END AS DECIMAL(10,0)) AS IND_107,
	
	CASE
		WHEN ULT_RT_SCD_DIR_DT IS NULL THEN 'RTI000_1'
		ELSE ''
	END AS ERROR_MSG_IND_107
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;