VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_108.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
CAST(
	CASE
		WHEN IMP_ARRETRATO_DIR IS NULL THEN NULL
		ELSE IMP_ARRETRATO_DIR
	END AS DECIMAL(18,6)) AS IND_108,
	
	CASE
		WHEN IMP_ARRETRATO_DIR IS NULL THEN 'RTI000_3'
		ELSE ''
	END AS ERROR_MSG_IND_108
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;