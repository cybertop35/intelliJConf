VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_109.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
 
CAST(
	CASE
		WHEN NUM_COEF_IMPG_RIDMAV IS NULL THEN NULL
		ELSE NUM_COEF_IMPG_RIDMAV
	END AS DECIMAL(18,6)) AS IND_109,
	
	CASE
		WHEN NUM_COEF_IMPG_RIDMAV IS NULL THEN 'RTI000_1'
		ELSE ''
	END AS ERROR_MSG_IND_109
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;