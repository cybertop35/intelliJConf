VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_114.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "
CAST(
	CASE
		WHEN ESP_FL_FORBORNE_SCONFINO IS NULL THEN '0'
		ELSE '1'
	END AS CHAR(1)) AS IND_114,
	
	CASE
		WHEN ESP_FL_FORBORNE_SCONFINO IS NULL THEN 'ESP000_1'
		ELSE ''
	END AS ERROR_MSG_IND_114
";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql;