VAR formulaName = "[LIB_EWS_RETAIL.INDICATOR_112.fe] ";
DEBUGMESSAGE(formulaName, "START");

VAR sql = "";

sql = "

CAST(
	CASE
		WHEN NUM_RATE_IMP_CDQ IS NULL OR NUM_RATE_IMP_CDQ < 0 THEN NULL
		ELSE NUM_RATE_IMP_CDQ
	END AS DECIMAL(10,0)) AS IND_112,
	
	CASE
		WHEN NUM_RATE_IMP_CDQ IS NULL THEN 'CQU000_1'
		WHEN NUM_RATE_IMP_CDQ < 0 THEN 'CQU000_2'
		ELSE ''
	END AS ERROR_MSG_IND_112


";

DEBUGMESSAGE(formulaName, sql);
DEBUGMESSAGE(formulaName, "END");

return sql; 
